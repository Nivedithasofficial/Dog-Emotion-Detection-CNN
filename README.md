# Animal Emotion Detection using CNN

## Overview
This project implements a Convolutional Neural Network (CNN) to classify animal emotions from images using TensorFlow/Keras.

> **Note:** This repository was generated from the provided notebook. Update the dataset description, final accuracy, and sample outputs after reviewing the notebook.

## Project Structure

```
Animal-Emotion-Detection-CNN/
├── notebooks/
├── models/
├── src/
├── images/
├── results/
├── data/
├── requirements.txt
└── README.md
```

## Installation

```bash
pip install -r requirements.txt
```

## Run
Open the notebook in `notebooks/` to train or evaluate the model.

## Future Improvements
- Streamlit/Gradio deployment
- Hyperparameter tuning
- Transfer learning
- Confusion matrix and ROC curves
